# requirements.txt

# Para o servidor de API (api_server.py)
fastapi
uvicorn[standard]  # O [standard] inclui algumas otimizações para produção

# Para o gerador de token (usado pela API) e o agente
livekit-api~=1.0

# Para o agente de IA (agent_server.py)
livekit-agents~=1.0
livekit-plugins-openai~=1.0
livekit-plugins-elevenlabs~=1.0
livekit-plugins-silero~=1.0

# Para carregar variáveis de ambiente do arquivo .env
python-dotenv